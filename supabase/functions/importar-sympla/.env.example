# Segredos da Edge Function "importar-sympla" — NUNCA commitar valores reais.
# Configurar em produção via: supabase secrets set NOME=valor
SYMPLA_API_TOKEN=
SYMPLA_EVENT_ID=
# Disponibilizada automaticamente pelo runtime das Edge Functions do Supabase,
# listada aqui só para documentação.
SUPABASE_SERVICE_ROLE_KEY=
